{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.comment_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.comment_list'|trans }}</h1>
    {% if pagination is defined and pagination|length %}

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.comment_id'|trans, 'u.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.comment_name'|trans, 'u.name') }}</th>
            </tr>
            </thead>
            <tbody>
            {% for comment in pagination %}
                <tr>
                    <td>{{ comment.id }}</td>
                    <td>{{ comment.title}}</td>
                    <td>
                        <a href="{{ url('comment_view', {id: comment.id}) }}" title="{{ 'action.view'|trans }}">
                            {{ 'action.view'|trans }}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}

{% endblock %}