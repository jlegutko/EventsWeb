{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.my_events'|trans }}
{% endblock %}

{% block body %}

    <div class="container-fluid">
        <h3 class="list-h mt-5"><i class="material-icons br-lg-1 mb-4 ">event</i> {{  'label.my_events'|trans }}</h3>
        <div class="d-flex justify-content-center mt-5">
            {% if pagination is defined and pagination|length %}
                <table class="table table-striped table-bordered">
                    <thead>
                    <tr>
                        <th scope="col">{{ 'label.table_id'|trans }}</th>
                        <th scope="col">{{ 'label.event_name'|trans}}</th>
                        <th scope="col">{{ 'label.item_action'|trans }}</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for event in pagination %}
                        <tr>
                            <th scope="row">{{ event.id }}</th>
                            <td>{{ event.name}}, {{ event.place }}, {{ event.startDate|date("d/m/y") }}</td>
                            <td><a href="{{ url('event_view', {id: event.id}) }}">{{ 'label.go_to_event'|trans }}</a></td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            {% endif %}
        </div>
        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}