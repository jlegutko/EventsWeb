{% include 'default/_form.html.twig' with {
    page_title: 'title.discussion_edit',
    page_title_param: {'%id%': discussion.id},
    attr: {
        method: 'PUT',
        action: url('discussion_edit', {'id': discussion.id}),
    },
    back_to_list_path: 'discussion_index',
} %}