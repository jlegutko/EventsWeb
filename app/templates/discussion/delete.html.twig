{% include 'default/_form.html.twig' with {
    page_title: 'title.discussion_delete',
    page_title_param: {'%id%': discussion.id},
    attr: {
        method: 'DELETE',
        action: url('discussion_delete', {'id': discussion.id}),
    },
    back_to_list_path: 'discussion_index',
} %}