{% include 'default/_form.html.twig' with {
    page_title: 'title.group_delete',
    page_title_param: {'%id%': group.id},
    attr: {
        method: 'DELETE',
        action: url('group_delete', {'id': group.id}),
    },
    back_to_list_path: 'group_index',
} %}